{
  "name": "amplenote-zotero-integration",
  "version": "0.1.0",
  "description": "Amplenote plugin: connect a Zotero library to Amplenote notes - reference search, citation insertion, content sync, annotation import.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "node esbuild.js",
    "test": "cross-env NODE_OPTIONS=--experimental-vm-modules jest",
    "test:watch": "cross-env NODE_OPTIONS=--experimental-vm-modules jest --watch"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "esbuild": "^0.21.5",
    "jest": "^29.7.0"
  }
}
